tree("Root")
	sequence
	//escolhe um destino e se move repetidamente
		PickDestination(-100, -100)
		MoveToDestination
		PickDestination(-100, 100)
		MoveToDestination
		PickDestination(100, 100)
		MoveToDestination
		PickDestination(100, -100)
		MoveToDestination
